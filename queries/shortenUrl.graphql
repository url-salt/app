mutation shortenUrl($url: String!, $settings: ShortenerSettings) {
    shortenUrl(url: $url, settings: $settings) {
        id
        originalUrl
        uniqueId
        createdAt
        updatedAt
        url
    }
}
