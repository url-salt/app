query url($id: String!) {
    url(id: $id) {
        id
        url
        uniqueId
        originalUrl
        title
        hits
        description
        createdAt
        visitLogs(take: 1) {
            id
            createdAt
        }
    }
}
